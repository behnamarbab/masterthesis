\begin{algorithm}
    \KwIn{\textbf{$in-memory test case$}}
    \If{\textcolor{red}{is\_exhaustive()} OR has\_new\_bits()}{
        SAVE the case into queue directory
    }
    \If{hanged}{
        SAVE the case into hangs directory
    }
    \If{crashed}{
        SAVE the case into crashes directory
    }
    \label{algo:waffle-save-if-interesting}
\end{algorithm}